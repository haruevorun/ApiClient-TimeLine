# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'APIClient+TimeLine' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for APIClient+TimeLine
  pod 'Alamofire'
  pod 'AlamofireImage'

  target 'APIClient+TimeLineTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'APIClient+TimeLineUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
